# Postgres connection string (Supabase, Neon, Railway, or local Postgres all work)
DATABASE_URL="postgresql://user:password@localhost:5432/storefront"

# Anthropic API key — used by the intake, generation, and (future) optimizer agents
ANTHROPIC_API_KEY="sk-ant-..."
